#!/bin/sh -eu

cd "$(dirname "$0")/.."

nix run \
    --ignore-environment \
    --file build.nix \
    infra.np.nixpkgs-stable.nix-project-exe \
    --command \
    nix-project \
    --upgrade \
    --source-dir sources \
    --nix "$(command -v nix)" \
    "$@"
