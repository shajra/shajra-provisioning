colors:

{
    focused = {
        border      = colors.window.border.tabs;
        background  = colors.window.selected.focused.background;
        text        = colors.window.selected.focused.text;
        indicator   = colors.window.selected.focused.indicator;
        childBorder = colors.window.selected.focused.border.window;
    };
    focusedInactive = {
        border      = colors.window.border.tabs;
        background  = colors.window.selected.unfocused.background;
        text        = colors.window.selected.unfocused.text;
        indicator   = colors.window.selected.unfocused.indicator;
        childBorder = colors.window.selected.unfocused.border.window;
    };
    unfocused = {
        border      = colors.window.border.tabs;
        background  = colors.window.unselected.background;
        text        = colors.window.unselected.text;
        indicator   = colors.window.unselected.indicator;
        childBorder = colors.window.unselected.border.window;
    };
    urgent = {
        border      = colors.window.border.tabs;
        background  = colors.window.urgent.background;
        text        = colors.window.urgent.text;
        indicator   = colors.window.urgent.indicator;
        childBorder = colors.window.urgent.border.window;
    };
    placeholder = {
        border      = colors.window.border.tabs;
        background  = colors.window.placeholder.background;
        text        = colors.window.placeholder.text;
        indicator   = colors.window.placeholder.indicator;
        childBorder = colors.window.placeholder.border;
    };
}
